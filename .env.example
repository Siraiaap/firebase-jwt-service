# =========================
# SiraIA — Auth + Credits
# Archivo de ejemplo. NO poner secretos reales aquí.
# =========================

# --- App ---
PORT=3000
BASE_URL=https://siraia-auth-credits.onrender.com
FRONTEND_URL=https://siraia.com
CORS_ORIGIN=https://siraia.com

# --- Auth / JWT ---
JWT_SECRET=REEMPLAZA_CON_LLAVE_SEGURA
JWT_TTL=30d
DEFAULT_COUNTRY=MX
INITIAL_CREDITS=10

# --- Firestore / Firebase Admin ---
# Opción A (recomendada en Render): monta el JSON del service account
# y apunta a su ruta absoluta dentro del contenedor.
GOOGLE_APPLICATION_CREDENTIALS=/etc/secrets/gcp-service-account.json

# Nombres de colecciones (puedes dejar los defaults)
FIRESTORE_COLLECTION_USERS=users
FIRESTORE_COLLECTION_ORDERS=orders
FIRESTORE_COLLECTION_EVENTS=stripe_events

# --- Stripe (modo TEST primero) ---
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# IDs de Price por región y paquete (ejemplos)
PRICE_ID_MX_10=price_mx_10_test
PRICE_ID_INTL_10=price_intl_10_test
PRICE_ID_MX_30=price_mx_30_test
PRICE_ID_INTL_30=price_intl_30_test

# --- Webhook interno / automatizaciones ---
WH_INTERNAL_KEY=REEMPLAZA_CON_CLAVE_INTERNA

# --- Notas ---
# 1) No subas nunca un .env con secretos reales al repositorio.
# 2) En Render, define estas variables en Dashboard -> Environment.
# 3) Para local, crea un archivo .env (no se commitea) a partir de este ejemplo.
